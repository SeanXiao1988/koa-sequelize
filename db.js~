var Sequelize = require('sequelize');
var sequelize = new Sequelize('test', '', '', {
  host: 'localhost',
  dialect: 'sqlite',

  pool: {
    max: 5,
    min: 0,
    idle: 10000
  },
  storage: 'test.sqlite',
  
});

module.exports = sequelize;
